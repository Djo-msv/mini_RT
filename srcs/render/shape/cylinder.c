#include "miniRT.h"

// float hit_cylinder(t_cylinder *cy, t_ray r)
// {
// 	float	t;
// 	float	a;
// 	float	b;
// 	float	c;
// 	float	delta;
//     t_vec   center = (t_vec) {cy->coordinate.x, cy->coordinate.y, cy->coordinate.z};

//     a = scalar_product(vec_sub(r.direction, vec_mul(center, scalar_product(r.direction, center))), );
// 	if (t >= 0.0)
// 		return (t);
// 	return (-1.0);
// }

